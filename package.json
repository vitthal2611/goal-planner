{
  "name": "goal-planner",
  "version": "1.0.0",
  "type": "module",
  "description": "Life Tracker - Finance and Habit Management App",
  "main": "public/index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:firestore": "firebase deploy --only firestore",
    "deploy:rules": "firebase deploy --only firestore:rules,storage:rules",
    "serve": "firebase serve",
    "init": "firebase init",
    "login": "firebase login",
    "logout": "firebase logout",
    "use": "firebase use --add",
    "emulators": "firebase emulators:start --only auth,firestore,hosting",
    "backup": "firebase firestore:export gs://your-project-backup/$(date +%Y%m%d_%H%M%S)",
    "test": "echo 'No tests specified'"
  },
  "keywords": [
    "firebase",
    "finance",
    "habits",
    "tracker",
    "budget",
    "atomic-habits"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "firebase-tools": "^12.9.1",
    "vite": "^5.0.8"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-is": "^19.2.4",
    "recharts": "^3.8.0"
  }
}
